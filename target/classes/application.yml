spring:
  datasource:
    url: jdbc:mysql://localhost:3307/acarb_db
    username: root
    password: MySQL@2022
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080
  spring:
    security:
      enabled: false